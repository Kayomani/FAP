<Window x:Class="EmailClient.Presentation.Views.EmailAccountsWizard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:EmailClient.Applications.ViewModels;assembly=EmailClient.Applications"
    mc:Ignorable="d" d:DataContext="{d:DesignInstance vm:EmailAccountsViewModel}"
    Title="Email Accounts" Height="400" Width="550" ShowInTaskbar="False">
    
    <DockPanel>
        <Border DockPanel.Dock="Bottom" Background="{x:Static SystemColors.ControlBrush}">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,11,11">
                <Button Content="Back" Command="{Binding BackCommand}"/>
                <Button Content="Next" Command="{Binding NextCommand}"/>
                <Button Content="Cancel" Command="{Binding CancelCommand}" Margin="22,11,0,0"/>
            </StackPanel>
        </Border>

        <ContentPresenter Content="{Binding ContentView}"/>        
    </DockPanel>    
</Window>
