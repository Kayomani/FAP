<UserControl x:Class="EmailClient.Presentation.Views.CreateEmailAccountView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:EmailClient.Applications.ViewModels;assembly=EmailClient.Applications"
    mc:Ignorable="d" d:DataContext="{d:DesignInstance vm:CreateEmailAccountViewModel}"
    HorizontalAlignment="Left" VerticalAlignment="Top">
    
    <Grid Margin="0,0,11,11">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Label Content="Create Account:" Grid.Column="0" Grid.Row="0" Style="{StaticResource HeaderLabel}" Margin="11,11,0,11"/>
        <RadioButton Content="Internet Email (POP3, SMTP)" IsChecked="{Binding IsPop3Checked}" Grid.Column="0" Grid.Row="1"/>
        <RadioButton Content="Microsoft Exchange" IsChecked="{Binding IsExchangeChecked}" Grid.Column="0" Grid.Row="2"/>
    </Grid>
    
</UserControl>
